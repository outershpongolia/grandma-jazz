export enum ERoute {
    HOME = '/',
    FLOWERS = '/flowers',
    REFRESHMENTS = '/refreshments',
    VIBE = '/vibe',
    VISION = '/vision'
}

export const defaultSlideUpAnimation = {
    translateY: [10, 0],
    opacity: [0, 1],
    easing: 'easeInQuad',
    duration: 800
}
